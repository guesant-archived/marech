import { IConfigRule } from "../types/IConfigRule";
import { IRulesPresetOptions } from "../types/IRulesPresetOptions";

export const generateRules = ({}: IRulesPresetOptions) => {
  const rules: IConfigRule[] = [];

  return rules;
};
